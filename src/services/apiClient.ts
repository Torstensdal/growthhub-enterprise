export const apiClient = {
  get: async (url: string) => {
    console.warn("apiClient.get stub:", url);
    return null;
  },
  post: async (url: string, _________body: any) => {
    console.warn("apiClient.post stub:", url);
    return null;
  },
  put: async (url: string, _________body: any) => {
    console.warn("apiClient.put stub:", url);
    return null;
  },
  delete: async (url: string) => {
    console.warn("apiClient.delete stub:", url);
    return null;
  }
};
